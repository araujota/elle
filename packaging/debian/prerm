#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if [ -d /run/systemd/system ]; then
            systemctl stop elled.service || true
            systemctl disable elled.service || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
